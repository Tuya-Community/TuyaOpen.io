# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.idea
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

package-lock.json

# vscode
.vscode

# Agent planning workspace (persistent, not committed)
agent-plan-workspace/

# TuyaOpen SDK (clone separately; see README — AI-assisted documentation and TuyaOpen SDK)
TuyaOpen/

# Local reference code (not part of the site)
reference_code/

# Dev-board catalog — generated from external/tuyaopen-ide-manifests submodule
# by scripts/sync-dev-boards.js (runs via the build/start/deploy scripts). The
# submodule is the source of truth; these artifacts are rebuilt locally and in CI.
src/data/devBoards.js
/static/dev-boards-data/
/static/img/boards/
